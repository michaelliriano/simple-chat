import { styled } from '@mui/system';

const StyledChatBox = styled('div')({
  padding: '1rem',
  maxHeight: '82vh',
  width: '80vw',
  overflowY: 'auto',
});

const StyledForm = styled('form')({});

export { StyledChatBox, StyledForm };
