import { styled } from '@mui/system';

const StyledSidebarFooter = styled('div')({
  position: 'absolute',
  left: 0,
  bottom: 0,
  width: '100%',
  padding: '0 .5rem',
  zIndex: 2,
});

export { StyledSidebarFooter };
